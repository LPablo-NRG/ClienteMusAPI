<UserControl x:Class="ClienteMusAPI.UserControls.ucSeleccionarListaPopup"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:ClienteMusAPI.UserControls"
             Background="#80000000">
    <!-- Fondo oscuro semitransparente -->

    <Grid>
        <Border Width="500" Height="400" Background="White" CornerRadius="10" Padding="10" 
                VerticalAlignment="Center" HorizontalAlignment="Center">
            <StackPanel>
                <TextBlock Text="Selecciona una lista" FontSize="20" FontWeight="Bold" Margin="0,0,0,10"/>
                <ScrollViewer VerticalScrollBarVisibility="Auto" Height="300">
                    <StackPanel x:Name="sp_Listas"/>
                </ScrollViewer>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>



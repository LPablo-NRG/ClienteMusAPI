<Window x:Class="ClienteMusAPI.VentanaPrincipal"
                xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                xmlns:local="clr-namespace:ClienteMusAPI"
                WindowState="Maximized"
                
                Title="MusAPI" Height="720" Width="1280">
    <Grid Background="White">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        <!-- Label fijo (parte estática) -->

        <Grid x:Name="Reproductor"  VerticalAlignment="Bottom" Height="70" Grid.Row="1" Visibility="Collapsed">
            <Grid.Resources>
                <!-- Declaro el brush con Key (no Name) -->
                <LinearGradientBrush x:Key="MovingBrush"
StartPoint="0,0" EndPoint="1,1">
                    <LinearGradientBrush.RelativeTransform>
                        <TranslateTransform />
                    </LinearGradientBrush.RelativeTransform>
                    <GradientStop Color="#ffd166" Offset="0"/>
                    <GradientStop Color="#ffd166" Offset="1.0"/>
                </LinearGradientBrush>
            </Grid.Resources>
            <Grid.Background>
                <StaticResource ResourceKey="MovingBrush"/>
            </Grid.Background>
            <Grid.Triggers>
                <EventTrigger RoutedEvent="Loaded">
                    <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimation
               Storyboard.TargetName="Reproductor"
               Storyboard.TargetProperty="(Background)
                                          .(LinearGradientBrush.RelativeTransform)
                                          .(TranslateTransform.X)"
               From="-.2" To="0.5"
               Duration="0:0:4"
               RepeatBehavior="Forever"
               AutoReverse="True"/>
                        </Storyboard>
                    </BeginStoryboard>
                </EventTrigger>
            </Grid.Triggers>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Label Content="Canción - Artista" 
                HorizontalAlignment="Center" VerticalAlignment="Center"
                FontSize="26" FontWeight="UltraBold" Foreground="#205781" Grid.Column="0"/>

            <Label Content="1:34 ----*---- 3:06"
                HorizontalAlignment="Center" VerticalAlignment="Center"
                FontSize="26" FontWeight="UltraBold" Foreground="#205781" Grid.Column="1"/>

            <StackPanel Orientation="Horizontal"
                HorizontalAlignment="Center" VerticalAlignment="Center"
                Grid.Column="2">


                <Button Grid.Column="2"
                    Width="40" Height="40"
                    VerticalAlignment="Center" HorizontalAlignment="Center"
                
                    Background="Transparent"
                    BorderBrush="Transparent"
                    Cursor="Hand">
                        <Image Source="Recursos\Iconos\iconoReproducir.png" />
                </Button>
                <Button Grid.Column="2"
                    Width="40" Height="40"
                    VerticalAlignment="Center" HorizontalAlignment="Center"
                
                    Background="Transparent"
                    BorderBrush="Transparent"
                    Cursor="Hand">
                        <Image Source="Recursos\Iconos\iconoPausa.png" />
                </Button>
                <Button Grid.Column="2"
                    Width="40" Height="40"
                    VerticalAlignment="Center" HorizontalAlignment="Center"
                
                    Background="Transparent"
                    BorderBrush="Transparent"
                    Cursor="Hand">
                        <Image Source="Recursos\Iconos\iconoSiguiente.png" />
                </Button>
                <Button Grid.Column="2"
                    Width="40" Height="40"
                    VerticalAlignment="Center" HorizontalAlignment="Center"

                    Background="Transparent"
                    BorderBrush="Transparent"
                    Cursor="Hand">
                        <Image Source="Recursos\Iconos\iconoVolumen.png" />
                </Button>
            </StackPanel>
        </Grid>

        <!-- Frame para navegar entre páginas (parte dinámica) -->
        <Frame x:Name="MarcoPrincipal" NavigationUIVisibility="Hidden" Grid.Row="0"
               HorizontalAlignment="Stretch"
               VerticalAlignment="Stretch" />
    </Grid>
</Window>
